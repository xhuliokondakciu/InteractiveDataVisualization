var KGT;
(function (KGT) {
    var Constants = /** @class */ (function () {
        function Constants() {
        }

        Constants.permissions = {
            view: "View",
            edit: "Edit",
            create: "Create",
            delete: "Delete",
            copy: "Copy"
        };

        return Constants;
    }());

    KGT.Constants = Constants;
})(KGT || (KGT = {}));